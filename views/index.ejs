<!DOCTYPE html>
<html lang="en">
<head>
    <meta charset = "UTF-8">
    <meta name="viewport" content="width=device-width, initial-scale=1.0">
<title>Welcome</title>
<link rel="stylesheet" href="/styles.css">
</head>
<body>

<h1>Welcome, here is my resume and some facts about me</h1>
<p>I enjoy singing/songwriting</p>
<p>I do photography in my spare time</p>
<div>
    <form action="/add" method="POST">
        <input type="text" name="newTask" placeholder="Add a new task"/>
        <button>
            Add Task
        </button>
    </form>
</div>
<div>
    <form action="/done" method="POST">
        <% for(var i = 0; i < tasks.length; i++){ %>
            <li> 
                <% if(tasks[i].done){ %>
                <input type="checkbox" name="task" value="<%=tasks[i]._id%>"checked="<%=tasks[i].done%>">
                <% } else {%>
                    <input type ="checkbox" name="task" value="<%=tasks[i]._id%>">
                    <% } %>
                <%=tasks [i].taskName%></li>
        <% } %>
        <button>
            Complete Task
        </button>
    </form>
</div>
</body>
</html>